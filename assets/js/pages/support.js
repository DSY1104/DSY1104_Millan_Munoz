// JS modular para la página de soporte
// Importa e inicializa la interactividad desde support-interactive.js

import { initSupportInteractive } from "./support-interactive.js";

document.addEventListener("DOMContentLoaded", () => {
  initSupportInteractive();
});
